# Frontend Development Standards

---

Taking the [Plugin Creation section](../create.md){style="color: green;" .custom-class #custom-id} as an example, the frontend directory follows these standards:

1. The `plugin/test/demo/web` directory is the frontend directory
2. All files in this directory will be copied (overwriting if the file exists) to the specified frontend directory during plugin installation